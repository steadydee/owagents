# Rotate Quote Token

1. Rotate the quote intake token in Operations/Vercel.
2. Update `QUOTE_INTAKE_API_TOKEN` in the live `owlswatch_quotes` MCP runtime environment.
3. Do not write the token into this repo.
4. Restart the `owlswatch` gateway if needed.
5. Run a Cotiza smoke test.

