# Users

Dennis is the primary receipt submitter.

Adriana is the secondary receipt submitter.

Both users are authorized through the `channels.telegram.allowFrom` allowlist in `~/.openclaw-owlswatch/openclaw.json`.
